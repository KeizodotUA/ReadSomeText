// Absolute imports
import React from 'react'

// Components
import Layout from '../Layout'

// Shared
import TodoPage from '@shared/TodoPage'

const DailyRating = () => {
    return (
        <TodoPage/>
    )
}

export default Layout()(DailyRating);