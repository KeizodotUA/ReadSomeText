// Absolute imports
import React from 'react'

// Components
import Layout from '../Layout'

const Home = () => {
    return (
        <div>
            HOME PAGE
        </div>
    )
}

export default Layout()(Home);