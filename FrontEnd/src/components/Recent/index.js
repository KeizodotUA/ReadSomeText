// Absolute imports
import React from 'react'

// Components
import Layout from '../Layout'

const Recent = () => {
    return (
        <div>
            Coming soon...
        </div>
    )
}

export default Layout()(Recent);