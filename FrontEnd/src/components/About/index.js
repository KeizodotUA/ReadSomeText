// Absolute imports
import React from 'react'

// Components
import Layout from '../Layout'

const About = () => {
    return (
        <div>
            About us...
        </div>
    )
}

export default Layout()(About);
